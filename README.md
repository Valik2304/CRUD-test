### Встановлення

встановити php 8.0+

встановити MySQL

встановити composer

### Або локальний сервер(OpenServer)

git clone

composer install

define APP_KEY in .env

php artisan migrate --seed

php artisan serve
